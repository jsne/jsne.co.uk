/**
 * @module LoadingScreen
 * @desc portal overlay for page load
 */

import React, { portal } from 'React';

import Root from '../shared/Root';

const className = '';

const LoadingScreen = () => (
    <Root className={className}>Hello world</Root>
);

export default LoadingScreen;
